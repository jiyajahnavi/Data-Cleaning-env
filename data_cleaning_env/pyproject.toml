[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data-cleaning-env"
version = "0.1.0"
description = "DataCleaningEnv for OpenEnv framework"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests"
]
